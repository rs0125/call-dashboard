# dependencies
node_modules
.pnp
.pnp.*

.claude

# next.js
.next
out
build

# production / misc
*.tsbuildinfo
next-env.d.ts
.DS_Store

# env / secrets — never commit
.env
.env.*
!.env.example

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Seed SQL contains real employee PII (names, phone numbers, emails,
# Twenty user IDs). Keep the real seeds out of git — commit the sanitized
# *.example.sql templates instead.
/prisma/seed-*.sql
!/prisma/seed-*.example.sql

# Internal audit/backlog notes — keep local
AUDIT.md
